package patterns.structure.PageObject;

import org.openqa.selenium.WebDriver;
import patterns.BaseProfilePageObject;

public class ProfilePage extends BaseProfilePageObject {
    public ProfilePage(WebDriver driver) {
        super(driver);
    }
}
