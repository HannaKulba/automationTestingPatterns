package patterns.data.ObjectPool;

import org.openqa.selenium.WebDriver;
import patterns.BaseProfilePageObject;

public class ProfilePageObjectPool extends BaseProfilePageObject {
    public ProfilePageObjectPool(WebDriver driver) {
        super(driver);
    }
}
