package patterns.technical.Proxy;

import org.openqa.selenium.WebDriver;
import patterns.BaseProfilePageObject;

public class ProfilePageProxy extends BaseProfilePageObject {
    public ProfilePageProxy(WebDriver driver) {
        super(driver);
    }
}
