package patterns.technical.Decorator;

import org.openqa.selenium.WebDriver;
import patterns.BaseProfilePageObject;

public class ProfilePageDecorator extends BaseProfilePageObject {
    public ProfilePageDecorator(WebDriver driver) {
        super(driver);
    }
}
